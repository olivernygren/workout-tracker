import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
	title: {
		position: 'absolute',
		top: 34,
		left: 20,
		zIndex: 11,
		textTransform: 'capitalize',
	},
}));
